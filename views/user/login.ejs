<!DOCTYPE html>
<html lang="en">
    
    <link rel="stylesheet" href=".././css/login.css"/>
    <%- include("../partials/head") %>

<body>
    
    <%- include("../partials/header") %>
    
    <main>

        <h3 class="titulos">Ingresa con tu usuario</h3>
        <div class="flex-contenedor">   
         
            <% if (typeof errors != "undefined") { %>
            <p style="color:red">ERROR</p> 
            <ul>
                <% for(let i=0; i < errors.length; i++) { %>
                    <li>
                        <%=errors[i].msg %> 
                    </li>
                <% } %>  
            </ul>
            <% } %> 

            <form id="formulario" action="/user/login" method="POST">
                
                <div class="formularios">
                    <input class="input-formulario" type="email" name="email" placeholder="Email">
                </div>
            
                <div class="formularios">
                    <input class="input-formulario" type="password" name="clave" placeholder="Clave">
                </div>

                <p class="boton-ingreso">
                    <button class="btn btn-success" type="submit">Ingresar</button>
                </p>
                
            </form>

            <p class="boton-ingreso">
                <a href="../user/registro"><button class="btn btn-primary" type="button">Registrarse</button></a>
            </p>

        </div>
    </main>

    <%- include("../partials/footer") %>
    
</body>
</html>